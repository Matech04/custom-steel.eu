---
import type { GetStaticPaths } from "astro";
import MainLayout from "../../layouts/main_layout.astro";

export async function getStaticPaths() {
  return [{ params: "pl" }, { params: "en" }, { params: "de" }];
}
---

<MainLayout />
